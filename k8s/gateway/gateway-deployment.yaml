apiVersion: apps/v1
kind: Deployment
metadata:
  name: blazorcrud-gateway
spec:
  replicas: 1
  selector:
    matchLabels:
      app: blazorcrud-gateway
  template:
    metadata:
      labels:
        app: blazorcrud-gateway
    spec:
      imagePullSecrets:
        - name: ghcr-secret
      containers:
        - name: blazorcrud-gateway
          image: ghcr.io/paawanx/blazorcrud.gateway:2
          imagePullPolicy: Always
          ports:
            - containerPort: 80
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: Development
